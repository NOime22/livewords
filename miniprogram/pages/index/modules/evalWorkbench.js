// =============================================================================
//  LiveWords · evalWorkbench module (Public Showcase Stub)
// =============================================================================
//
//  The original file is the developer-mode eval workbench mounted on the
//  Index page. It exposes chain switching, single-episode triggering, and
//  live promptMeta inspection. Its full implementation depends on private
//  rubric definitions and chain configurations.
//
//  📖  Methodology overview: docs/eval-methodology.md
//
// =============================================================================

module.exports = {
  // No-op stubs so the Index page can keep referencing the module without
  // crashing in the public build. The real workbench is private.
  attachEvalWorkbench() {},
  refreshEvalWorkbench() {},
  isEvalWorkbenchAvailable() {
    return false;
  },
};
